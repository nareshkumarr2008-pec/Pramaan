# Fine-tuning pipeline requirements. Install with:
#   pip install -r requirements.txt
# Needs a CUDA-capable GPU (8GB+ VRAM recommended for the 3B base model in 4-bit).

torch>=2.3.0
unsloth @ git+https://github.com/unslothai/unsloth.git
trl>=0.9.6
transformers>=4.43.0
datasets>=2.20.0
peft>=0.11.0
bitsandbytes>=0.43.0
accelerate>=0.31.0
